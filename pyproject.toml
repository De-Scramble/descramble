[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "descramble"
version = "0.1.0"
description = "Probabilistic identity resolution into an Apache Iceberg lakehouse"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = [
    "entity-resolution",
    "record-linkage",
    "fellegi-sunter",
    "splink",
    "apache-iceberg",
    "lakehouse",
    "data-engineering",
    "deduplication",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Database",
    "Topic :: Scientific/Engineering :: Information Analysis",
]

# These are the libraries the pipeline imports, declared so the package is
# well-formed and introspectable.
#
# They are NOT the recommended way to install De-Scramble. Resolving them pulls
# splink's own hard dependency on igraph, which is GPL-licensed and which this
# project never uses. The recommended install is the pinned, igraph-free
# manifest in requirements.txt installed with --no-deps; see README "Installing"
# and note 2 in NOTICE. pip has no mechanism to exclude a transitive package, so
# the exclusion cannot be expressed here.
dependencies = [
    "splink>=4.0,<5",
    "duckdb>=1.0",
    "pyiceberg[sql-sqlite]>=0.9",
    "pyarrow>=15",
    "pandas>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
descramble = "descramble.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning",
]
